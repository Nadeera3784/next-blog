export const DEFAULT_LOGIN_REDIRECT: string = "/dashboard";

export const PUBLIC_ROUTES: string[] = ["/"];

export const AUTH_ROUTES: string[] = ["/auth/sign-in", "/auth/error"];

export const API_AUTH_PREFIX: string = "/api/auth";
